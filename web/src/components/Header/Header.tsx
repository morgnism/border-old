import HeroBanner from '@components/Hero';
import HeaderNav from './HeaderNav';

const Header = () => {
  return (
    <header>
      <HeaderNav />
      <HeroBanner />
    </header>
  );
};

export default Header;
