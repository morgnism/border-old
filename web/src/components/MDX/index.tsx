import Callout from './Callout';
import CodeBlock from './CodeBlock';

const MDXComponents = {
  // h1: (props) => <></>,
  // h2: (props) => <></>,
  // h3: (props) => <></>,
  // h4: (props) => <></>,
  // hr: (props) => <></>,
  // strong: (props) => <></>,
  code: CodeBlock,
  // pre: (props) => <></>,
  Callout,
};

export default MDXComponents;
